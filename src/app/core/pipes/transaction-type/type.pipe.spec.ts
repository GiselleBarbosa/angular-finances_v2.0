/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';

import { TypePipe } from './type.pipe';

describe('Pipe: Categorye', () => {
  it('create an instance', () => {
    let pipe = new TypePipe();
    expect(pipe).toBeTruthy();
  });
});
